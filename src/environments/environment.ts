/**
 * Production environment — replace with your Supabase credentials.
 * This file is git-ignored in CI; use environment.example.ts as a template.
 */
export const environment = {
  production: true,
  supabaseUrl: 'YOUR_SUPABASE_URL',
  supabaseAnonKey: 'YOUR_SUPABASE_ANON_KEY',
};
